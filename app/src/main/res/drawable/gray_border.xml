<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#ffffff" /> <!-- Màu nền (trắng trong trường hợp này) -->
    <corners android:radius="10dp" /> <!-- Độ cong của góc (20dp trong trường hợp này) -->
    <stroke
        android:width="2dp"
        android:color="@color/grey" />
</shape>